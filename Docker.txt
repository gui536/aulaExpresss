docker run -e "ACCEPT_EULA=Y" -e "MSSQL_SA_PASSWORD=sa123456!" \
   -p 1433:1433 --name sql1 --hostname sql1 \
    -d \
    mcr.microsoft.com/mssql/server:2017-latest

sudo docker exec -it sql1 /opt/mssql-tools/bin/sqlcmd -S localhost -U sa -P  sa123456!

/opt/mssql-tools/bin/sqlcmd -S localhost -U SA -P sa123456!





Docker MYQSL

docker pull mysql/mysql-server:latest

docker run --name mysql-has -e MYSQL_ROOT_PASSWORD=sa123456! -d mysql:latest

docker run -it --network some-network --rm mysql mysql -h mysql-has -u sa -p sa123456!


docker exec -it mysql-has bash



Run new container:

docker run --name=mysql mysql/mysql-server:latest
check password